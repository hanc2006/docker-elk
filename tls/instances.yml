# This file is used by elasticsearch-certutil to generate X.509 certificates
# for stack components.
#
# Ref. https://www.elastic.co/guide/en/elasticsearch/reference/current/certutil.html#certutil-silent
instances:

- name: elasticsearch
  dns:
  - elasticsearch.elk-stack.orb.local
  - elasticsearch
  - localhost
  ip:
  - 127.0.0.1
  - ::1

- name: kibana
  dns:
  - kibana.elk-stack.orb.local
  - kibana
  - localhost
  ip:
  - 127.0.0.1
  - ::1

- name: fleet-server
  dns:
  - fleet-server.elk-stack.orb.local
  - fleet-server
  - localhost
  ip:
  - 127.0.0.1
  - ::1

- name: apm-server
  dns:
  - apm-server.elk-stack.orb.local
  - apm-server
  - localhost
  ip:
  - 127.0.0.1
  - ::1
